# ============================================
# IMPORTANT: Never commit this file with real values
# Rename to .env and fill with your actual credentials
# Keep .env in .gitignore (already configured)
# ============================================

# ============================================
# Azure / Microsoft Graph Configuration
# ============================================
# Your Azure AD Tenant ID
# Find in: Azure Portal > Azure Active Directory > App registrations > [Your App] > Directory (tenant) ID
TENANT_ID=your_azure_tenant_id_here

# Your Azure AD Application (Client) ID
# Find in: Azure Portal > Azure Active Directory > App registrations > [Your App] > Application (client) ID
CLIENT_ID=your_azure_client_id_here

# Your Azure AD Application Secret
# ⚠️ SECURITY: Keep this secret! Never commit to git.
# Find in: Azure Portal > Azure Active Directory > App registrations > [Your App] > Certificates & secrets
CLIENT_SECRET=your_azure_client_secret_here

# SharePoint Site ID
# Can be found using Microsoft Graph Explorer or SharePoint admin
SITE_ID=your_sharepoint_site_id_here

# SharePoint List ID (for articles/content)
# Can be found using Microsoft Graph Explorer or SharePoint admin
LIST_ID=your_sharepoint_list_id_here

# ============================================
# Webmentions Configuration (Optional)
# ============================================
# Your webmention.io API token
# Register at: https://webmention.io/
# ⚠️ SECURITY: Keep this secret! Never commit to git.
WEBMENTION_API_KEY=your_webmention_api_key_here

# ============================================
# Site Configuration (Usually set in src/site.config.ts)
# ============================================
# Your site domain (used by webmentions)
# SITE=https://federicotosetto.com/

# ============================================
# Notes
# ============================================
# 1. Copy this file to .env in the root directory
# 2. Fill in the actual values
# 3. Never commit .env to git (already in .gitignore)
# 4. Use different credentials for dev and production
# 5. Rotate secrets periodically for security
